package com.own.microservice.provider.friends.circle.timeline;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class ProviderFriendsCircleTimelineApplication {

    public static void main(String[] args) {
        SpringApplication.run(ProviderFriendsCircleTimelineApplication.class, args);
    }
}
